#if defined _bbq6_included
  #endinput
#endif
#define _bbq6_included

native fm_get_user_body(id);

native fm_set_user_body(id, points);

native fm_get_user_level(id);

native fm_set_user_xp(id, xp);

native fm_max_user_level();

native fm_set_user_item(id, item);

native fm_rem_user_item(id, item);